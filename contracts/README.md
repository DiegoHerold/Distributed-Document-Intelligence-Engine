# Contracts

Esta pasta e reservada para artefatos de contratos publicados, como JSON Schema e OpenAPI gerados a partir dos modelos compartilhados.

Na fase atual, os contratos vivos estao em `packages/document-core/src/eixo/core/contracts.py` e possuem geracao de JSON Schema via `json_schema()`.

